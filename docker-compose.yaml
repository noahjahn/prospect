services:
  prospect:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
      - '8443:8443'

  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
